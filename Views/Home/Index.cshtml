@model Dictionary<long, Lobby>

@{
    ViewData["Title"] = "lobbies";
}

<div class="text-center">
    @if (Model != null)
    {
        foreach(var lobby in Model)
        {
            <h3> @lobby.Value.match?.name </h3>
        }
    }
</div>
